import React from 'react';

const CuterButton: React.FC = () => {
	return (
		<button className='bg-zinc-900 p-2 px-6 rounded hover:bg-zinc-800 active:bg-zinc-700'>
			Cuter
		</button>
	);
};

export default CuterButton;
